let root = require(path.join(src,"metadata.json"));
treefyFolder(src,dest,root);

treefyFolder(src,dest,root){
    let ans = fs.lsatSyyc(src);
    if(ans == fasle){
        let oldFile = path.join(src,node.newName);
        let newFile = path.join(dest,node.name);
    } ellse {
        
    }
}